#include <stdio.h>
#include <stdlib.h>
int main(){
    // char str[10];
    // // gets(str);
    // fgets(str, 10, stdin);
    // printf("input - %s", str);
    // char c = getchar();
    // printf("- %c", c);
    
    // char str_i[10];
    // char str_f[10];
    // char str_l[10];
    
    // printf("Enter i :");
    // fgets(str_i, 10, stdin);
    // printf("Enter f :");
    // fgets(str_f, 10, stdin);
    // printf("Enter l :");
    // fgets(str_l, 10, stdin);

    // int i = atoi(str_i);
    // float f = atof(str_f);
    // long l = atol(str_l);
    // printf("%d - %f - %ld", i, f, l);

    // false
    // int found = 0;
    // float f2 = 0.0;
    // char c = '\0'; terminate character, null character ascii code 0
    // "kong" => ['k', 'o', 'n', 'g', '\0'];
    // true
    // char str[10];
    // fgets(str, 10, stdin);
    // int i = atoi(str);
    
    // Relational Operator > >= < <= == !=
    // no 10 < i < 20
    // Logical Operator && || !
    // if(i<300){
    //     printf("if %d", i<300);
    // }else if(i>=300 && i<1000){
    //     printf("else if %d", i>=300 && i<1000);
    // }else{
    //     printf("else %d %d", i<300, i<1000);
    // }

}
